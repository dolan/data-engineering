<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Data Engineering" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Data Engineering" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <div class="container-fluid">
        <div class="row">
          <div class="col-md-1 nav-gutter">
            &nbsp;
          </div>
          <div class="col-md-10">
            <%= render 'layouts/navigation' %>
          </div>
          <div class="col-md-1 nav-gutter">
            &nbsp;
          </div>
        </div>
      </div>
    </header>
    <main role="main">
       <div class="container-fluid">
         <div class="row">
           <div class="col-md-1 content-gutter">
             &nbsp;
           </div>
           <div class="col-md-10 main-content">
             <%= render 'layouts/messages' %>
             <%= yield %>
           </div>
           <div class="col-md-1 content-gutter">
             &nbsp;
           </div>
         </div>
       </div>
    </main>
  </body>
</html>
